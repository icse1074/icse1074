The current test suite achieves {coverage_pct}% line coverage.

The following lines in solution.py are not covered by any test:
{missing_lines}

Please add new tests to cover these missing lines. Keep all existing tests unchanged.

Current test suite:
{tests}

Rules:
- Output ONLY valid Python code, no explanations
- Keep ALL existing tests unchanged
- Add new tests that exercise the uncovered lines shown above
- Each new test must directly target one or more of the missing lines
- Tests MUST only fail with AssertionError — use direct assert statements
- All imports must be from solution.py