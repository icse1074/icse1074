The following function is from the file solution.py
Your goal is to generate unit tests that kill 100% of the provided mutants.

A mutant is considered KILLED if at least one test produces a different outcome on the mutant than on the original code (e.g. a passing test fails, or a failing test passes).
A mutant is ALIVE if all tests produce the same outcome on both the original and mutated code.
Analyze the mutant to generate tests to kill all of them.


Function under test:
{code}

Mutants to kill:
{mutants}

Return only the unit tests implementation.

Rules:
- Output ONLY valid Python code
- No extra explanations
- Avoid using parameterized tests
- You can write as many tests as you want
- Include all necessary imports including the function name from the file solution.py